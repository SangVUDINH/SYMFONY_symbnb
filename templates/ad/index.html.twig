{% extends 'base.html.twig' %}

{% block title %}Nos annonces{% endblock %}

{% block body %}
<div class="container">
    <h1 class=""> Voici les annonces de nos hotes !</h1>

    <div class="row">
        {% for ad in ads %}
            <div class="col-md-4">
                <div class="card bg-light mb-3">
                    <div class="card-header text-center">
                        {{ ad.rooms }}, <strong> {{ad.price}} euros / nuit </strong> <br>
                        <small> Pas encore noté </small>
                    </div>

                    <img src="{{ ad.coverImage}}" alt="Image de l'appartement" 
                    style="height: 200px; width 100%; display: block">

                    <div class="card-body">
                        <h4 class="card-title">
                        {{ad.title}}
                        </h4>
                        <p class="card-text">
                        {{ad.introduction}}
                        </p>
                        <button href='#' class="btn btn-secondary">Savoir plus</button>
                    </div>
                </div>
            </div>
        {% endfor %}
    
    </div>
</div>

{% endblock %}
